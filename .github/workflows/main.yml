name: Release

on:
  push:
    tags:
      - 'v*'  # Only build on version tags (v0.1.26, etc.)
  workflow_dispatch:  # Allow manual trigger

permissions:
  contents: write  # Required for creating releases and uploading artifacts

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  # Check if the tag is on main branch (we only want release builds here)
  check-branch:
    runs-on: ubuntu-latest
    outputs:
      on-main: ${{ steps.check.outputs.on-main }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch all history to check branches

      - name: Check if tag is on main branch
        id: check
        run: |
          # Check if the current tag is reachable from main branch
          if git merge-base --is-ancestor HEAD origin/main 2>/dev/null; then
            echo "on-main=true" >> $GITHUB_OUTPUT
            echo "✓ Tag is on main branch - proceeding with release"
          else
            echo "on-main=false" >> $GITHUB_OUTPUT
            echo "✗ Tag is NOT on main branch - skipping release"
          fi

  build-windows:
    needs: check-branch
    if: needs.check-branch.outputs.on-main == 'true' || github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/_build-platform.yml
    with:
      runner: windows-latest
      platform: windows
      branch_condition: ${{ needs.check-branch.outputs.on-main }}
      should_release: true
    secrets: inherit

  build-linux:
    needs: check-branch
    if: needs.check-branch.outputs.on-main == 'true' || github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/_build-platform.yml
    with:
      runner: ubuntu-latest
      platform: linux
      branch_condition: ${{ needs.check-branch.outputs.on-main }}
      should_release: true
    secrets: inherit

  build-macos:
    needs: check-branch
    if: needs.check-branch.outputs.on-main == 'true' || github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/_build-platform.yml
    with:
      runner: macos-latest
      platform: macos
      branch_condition: ${{ needs.check-branch.outputs.on-main }}
      should_release: true
    secrets: inherit
