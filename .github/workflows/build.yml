name: Build

on:
  push:
    tags:
      - 'v*'  # Only build on version tags (v0.1.26, etc.)
  workflow_dispatch:  # Allow manual trigger

permissions:
  contents: read  # No release creation needed, just build artifacts

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  # Check if the tag is NOT on main branch (we only want dev builds here)
  check-branch:
    runs-on: ubuntu-latest
    outputs:
      not-on-main: ${{ steps.check.outputs.not-on-main }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch all history to check branches

      - name: Check if tag is NOT on main branch
        id: check
        run: |
          # Check if the current tag is NOT reachable from main branch
          if git merge-base --is-ancestor HEAD origin/main 2>/dev/null; then
            echo "not-on-main=false" >> $GITHUB_OUTPUT
            echo "✗ Tag is on main branch - skipping build (will use Release workflow)"
          else
            echo "not-on-main=true" >> $GITHUB_OUTPUT
            echo "✓ Tag is NOT on main branch - proceeding with dev build"
          fi

  build-windows:
    runs-on: windows-latest
    needs: check-branch
    if: needs.check-branch.outputs.not-on-main == 'true' || github.event_name == 'workflow_dispatch'
    strategy:
      matrix:
        backend: [exrs, openexr]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2025-10-15

      - name: Restore cargo and build artifacts cache
        id: cache-windows
        uses: actions/cache/restore@v4
        with:
          path: |
            ~/.cargo/registry/
            ~/.cargo/git/
            ~/.cargo/bin/
            target/
          key: playa-windows-dev-${{ matrix.backend }}-v1
          restore-keys: |
            playa-windows-dev-${{ matrix.backend }}-

      - name: Fetch dependencies
        run: cargo fetch

      - name: Clean old installers
        run: |
          # Remove old installer artifacts from cache to prevent version mismatch
          # Keep playa.exe binary, only remove installer packages
          Remove-Item target/release/playa_*-setup.* -ErrorAction SilentlyContinue
          Remove-Item target/release/*.msi -ErrorAction SilentlyContinue

      - name: Install cargo-packager
        run: |
          if (!(Get-Command cargo-packager -ErrorAction SilentlyContinue)) {
            cargo install cargo-packager --version 0.11.7 --locked
          } else {
            Write-Host "cargo-packager already installed"
          }

      - name: Build the application (${{ matrix.backend }} backend)
        run: |
          if ("${{ matrix.backend }}" -eq "openexr") {
            cargo xtask build --openexr
          } else {
            cargo xtask build
          }

      - name: Package the application
        run: cargo packager --release

      - name: Rename installer to include backend
        shell: pwsh
        run: |
          $installer = Get-ChildItem -Path "target/release" -Filter "playa_*-setup.*" | Select-Object -First 1
          if ($installer) {
            $newName = $installer.Name -replace '(playa_[\d\.\-]+)', "`$1-${{ matrix.backend }}"
            Rename-Item -Path $installer.FullName -NewName $newName
            Write-Host "Renamed $($installer.Name) to $newName"
          }

      - name: Find installer
        id: find_installer
        shell: pwsh
        run: |
          $installer = Get-ChildItem -Path "target/release" -Filter "playa_*-setup.*" | Select-Object -First 1
          if ($installer) {
            $relativePath = "target/release/$($installer.Name)"
            echo "path=$relativePath" >> $env:GITHUB_OUTPUT
            echo "Found installer: $($installer.FullName)"
            echo "Using relative path: $relativePath"
          } else {
            echo "Installer not found in target/release"
            exit 1
          }

      - name: Create binstall ZIP
        shell: pwsh
        run: |
          # Create directory for binstall package
          New-Item -ItemType Directory -Force -Path "binstall" | Out-Null

          # Copy exe
          Copy-Item "target/release/playa.exe" -Destination "binstall/"

          # Copy all dll files
          Get-ChildItem -Path "target/release" -Filter "*.dll" | Copy-Item -Destination "binstall/"

          # Create zip with rust target name for cargo-binstall
          $zipName = "playa-${{ matrix.backend }}-x86_64-pc-windows-msvc.zip"
          Compress-Archive -Path "binstall/*" -DestinationPath "target/release/$zipName" -Force

          echo "Created binstall ZIP: $zipName"
          echo "Contents:"
          Get-ChildItem "binstall" | Format-Table Name, Length

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-${{ matrix.backend }}
          path: |
            ${{ steps.find_installer.outputs.path }}
            target/release/playa-${{ matrix.backend }}-x86_64-pc-windows-msvc.zip
          if-no-files-found: error
          retention-days: 7

      - name: Save cargo and build artifacts cache
        if: needs.check-branch.outputs.not-on-main == 'true' && steps.cache-windows.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: |
            ~/.cargo/registry/
            ~/.cargo/git/
            ~/.cargo/bin/
            target/
          key: playa-windows-dev-${{ matrix.backend }}-v1

  build-linux:
    runs-on: ubuntu-latest
    needs: check-branch
    if: needs.check-branch.outputs.not-on-main == 'true' || github.event_name == 'workflow_dispatch'
    strategy:
      matrix:
        backend: [exrs, openexr]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake libgtk-3-dev libxcb-render0-dev libxcb-shape0-dev libxcb-xfixes0-dev libspeechd-dev libxkbcommon-dev libssl-dev

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2025-10-15

      - name: Restore cargo and build artifacts cache
        id: cache-linux
        uses: actions/cache/restore@v4
        with:
          path: |
            ~/.cargo/registry/
            ~/.cargo/git/
            ~/.cargo/bin/
            target/
          key: playa-linux-dev-${{ matrix.backend }}-v1
          restore-keys: |
            playa-linux-dev-${{ matrix.backend }}-

      - name: Install cargo-packager
        run: cargo packager --version || cargo install cargo-packager --version 0.11.7 --locked

      - name: Fetch dependencies
        run: cargo fetch

      - name: Clean old installers
        run: |
          # Remove old installer artifacts from cache to prevent version mismatch
          rm -f target/release/*.AppImage target/release/*.deb

      - name: Build the application (${{ matrix.backend }} backend)
        run: |
          if [ "${{ matrix.backend }}" = "openexr" ]; then
            cargo xtask build --openexr
          else
            cargo xtask build
          fi

      - name: Package the application
        run: |
          export LD_LIBRARY_PATH="$(pwd)/target/release:$LD_LIBRARY_PATH"
          cargo packager --release

      - name: Rename installers to include backend
        run: |
          # Rename AppImage
          appimage=$(find target/release -maxdepth 1 \( -name "*.AppImage" -o -name "*.appimage" \) 2>/dev/null | head -n 1)
          if [ -n "$appimage" ]; then
            newname=$(echo "$appimage" | sed "s/\(playa[_-][0-9.]\+\)/\1-${{ matrix.backend }}/")
            mv "$appimage" "$newname"
            echo "Renamed AppImage: $(basename $appimage) -> $(basename $newname)"
          fi

          # Rename DEB
          deb=$(find target/release -maxdepth 1 -name "*.deb" 2>/dev/null | head -n 1)
          if [ -n "$deb" ]; then
            newname=$(echo "$deb" | sed "s/\(playa[_-][0-9.]\+\)/\1-${{ matrix.backend }}/")
            mv "$deb" "$newname"
            echo "Renamed DEB: $(basename $deb) -> $(basename $newname)"
          fi

      - name: List bundle contents (if successful)
        run: |
          if [ -d "target/release/bundle" ]; then
            ls -R target/release/bundle/
          else
            echo "Bundle directory not found - build likely failed"
            exit 0
          fi

      - name: Find AppImage
        id: find_appimage
        run: |
          # Prefer top-level target/release output from cargo-packager
          appimage=$(find target/release -maxdepth 1 \( -name "*.AppImage" -o -name "*.appimage" \) 2>/dev/null | head -n 1)
          # Fallback to legacy bundle path if needed
          if [ -z "$appimage" ]; then
            appimage=$(find target/release/bundle/appimage -name "*.AppImage" 2>/dev/null | head -n 1)
          fi
          if [ -n "$appimage" ]; then
            echo "path=$appimage" >> $GITHUB_OUTPUT
            echo "Found AppImage: $appimage"
          else
            echo "No AppImage found"
          fi

      - name: Find DEB
        id: find_deb
        run: |
          # Prefer top-level target/release output from cargo-packager
          deb=$(find target/release -maxdepth 1 -name "*.deb" 2>/dev/null | head -n 1)
          # Fallback to legacy bundle path if needed
          if [ -z "$deb" ]; then
            deb=$(find target/release/bundle/deb -name "*.deb" 2>/dev/null | head -n 1)
          fi
          if [ -n "$deb" ]; then
            echo "path=$deb" >> $GITHUB_OUTPUT
            echo "Found DEB: $deb"
          else
            echo "No DEB found"
          fi

      - name: Create binstall ZIP
        run: |
          # Create directory for binstall package
          mkdir -p binstall

          # Copy binary
          cp target/release/playa binstall/

          # Copy all .so files (including symlinks)
          cp -P target/release/*.so* binstall/ 2>/dev/null || true

          # Create zip with rust target name for cargo-binstall (preserve symlinks)
          cd binstall
          zip -ry ../target/release/playa-${{ matrix.backend }}-x86_64-unknown-linux-gnu.zip *
          cd ..

          echo "Created binstall ZIP: playa-${{ matrix.backend }}-x86_64-unknown-linux-gnu.zip"
          echo "Contents:"
          ls -lh binstall/

      - name: Upload Linux AppImage artifact
        if: steps.find_appimage.outputs.path != ''
        uses: actions/upload-artifact@v4
        with:
          name: linux-appimage-${{ matrix.backend }}
          path: ${{ steps.find_appimage.outputs.path }}
          retention-days: 7
          if-no-files-found: error

      - name: Upload Linux DEB artifact
        if: steps.find_deb.outputs.path != ''
        uses: actions/upload-artifact@v4
        with:
          name: linux-deb-${{ matrix.backend }}
          path: ${{ steps.find_deb.outputs.path }}
          retention-days: 7
          if-no-files-found: error

      - name: Upload Linux binstall artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-binstall-${{ matrix.backend }}
          path: target/release/playa-${{ matrix.backend }}-x86_64-unknown-linux-gnu.zip
          retention-days: 7
          if-no-files-found: error

      - name: Save cargo and build artifacts cache
        if: needs.check-branch.outputs.not-on-main == 'true' && steps.cache-linux.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: |
            ~/.cargo/registry/
            ~/.cargo/git/
            ~/.cargo/bin/
            target/
          key: playa-linux-dev-${{ matrix.backend }}-v1

  build-macos:
    runs-on: macos-latest
    needs: check-branch
    if: needs.check-branch.outputs.not-on-main == 'true' || github.event_name == 'workflow_dispatch'
    strategy:
      matrix:
        backend: [exrs, openexr]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: brew install cmake

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2025-10-15

      - name: Restore cargo and build artifacts cache
        id: cache-macos
        uses: actions/cache/restore@v4
        with:
          path: |
            ~/.cargo/registry/
            ~/.cargo/git/
            ~/.cargo/bin/
            target/
          key: playa-macos-dev-${{ matrix.backend }}-v1
          restore-keys: |
            playa-macos-dev-${{ matrix.backend }}-

      - name: Install cargo-packager
        run: cargo packager --version || cargo install cargo-packager --version 0.11.7 --locked

      - name: Import Code Signing Certificate
        if: env.APPLE_CERTIFICATE != ''
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
        run: |
          # Create temporary keychain
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
          KEYCHAIN_PASSWORD=$(openssl rand -base64 32)

          # Decode certificate
          echo "$APPLE_CERTIFICATE" | base64 --decode > $RUNNER_TEMP/certificate.p12

          # Create keychain
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          # Import certificate
          security import $RUNNER_TEMP/certificate.p12 \
            -P "$APPLE_CERTIFICATE_PASSWORD" \
            -k "$KEYCHAIN_PATH" \
            -T /usr/bin/codesign \
            -T /usr/bin/productsign

          # Allow codesign to use the key without prompting
          security set-key-partition-list -S apple-tool:,apple: \
            -k "$KEYCHAIN_PASSWORD" \
            "$KEYCHAIN_PATH"

          # Add to search list
          security list-keychain -d user -s "$KEYCHAIN_PATH"

          # Verify certificate is available
          security find-identity -v -p codesigning "$KEYCHAIN_PATH"

          echo "Certificate imported successfully"

      - name: Fetch dependencies
        run: cargo fetch

      - name: Clean old installers
        run: |
          # Remove old installer artifacts from cache to prevent version mismatch
          rm -f target/release/*.dmg

      - name: Build the application (${{ matrix.backend }} backend)
        run: |
          if [ "${{ matrix.backend }}" = "openexr" ]; then
            cargo xtask build --openexr
          else
            cargo xtask build
          fi

      - name: Package the application
        run: cargo packager --release

      - name: Rename DMG to include backend
        run: |
          # Rename DMG
          dmg=$(find target/release -maxdepth 1 -name "*.dmg" 2>/dev/null | head -n 1)
          if [ -n "$dmg" ]; then
            newname=$(echo "$dmg" | sed "s/\(playa[_-][0-9.]\+\)/\1-${{ matrix.backend }}/")
            mv "$dmg" "$newname"
            echo "Renamed DMG: $(basename $dmg) -> $(basename $newname)"
          fi

      - name: Find DMG
        id: find_dmg
        run: |
          # Prefer top-level target/release output from cargo-packager
          dmg=$(find target/release -maxdepth 1 -name "*.dmg" 2>/dev/null | head -n 1)
          # Fallback to legacy bundle path if needed
          if [ -z "$dmg" ]; then
            dmg=$(find target/release/bundle/dmg -name "*.dmg" 2>/dev/null | head -n 1)
          fi
          if [ -n "$dmg" ]; then
            echo "path=$dmg" >> $GITHUB_OUTPUT
            echo "Found DMG: $dmg"
          else
            echo "No DMG found"
          fi

      - name: Create binstall ZIP
        run: |
          # Create directory for binstall package
          mkdir -p binstall

          # Copy binary
          cp target/release/playa binstall/

          # Copy all .dylib files (including symlinks)
          cp -P target/release/lib*.dylib binstall/ 2>/dev/null || true

          # Create zip with rust target name for cargo-binstall (preserve symlinks)
          cd binstall
          zip -ry ../target/release/playa-${{ matrix.backend }}-aarch64-apple-darwin.zip *
          cd ..

          echo "Created binstall ZIP: playa-${{ matrix.backend }}-aarch64-apple-darwin.zip"
          echo "Contents:"
          ls -lh binstall/

      - name: Upload macOS DMG artifact
        if: steps.find_dmg.outputs.path != ''
        uses: actions/upload-artifact@v4
        with:
          name: macos-dmg-${{ matrix.backend }}
          path: ${{ steps.find_dmg.outputs.path }}
          retention-days: 7
          if-no-files-found: error

      - name: Upload macOS binstall artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-binstall-${{ matrix.backend }}
          path: target/release/playa-${{ matrix.backend }}-aarch64-apple-darwin.zip
          retention-days: 7
          if-no-files-found: error

      - name: Save cargo and build artifacts cache
        if: needs.check-branch.outputs.not-on-main == 'true' && steps.cache-macos.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: |
            ~/.cargo/registry/
            ~/.cargo/git/
            ~/.cargo/bin/
            target/
          key: playa-macos-dev-${{ matrix.backend }}-v1
