# Анализ и план интеграции GPU композера в Playa

## 📊 АНАЛИЗ ТЕКУЩЕЙ АРХИТЕКТУРЫ

### ✅ Что уже реализовано:

1. **CPU Compositor** (`src/entities/compositor.rs`):
   - Поддержка 3 пиксельных форматов: F32, F16, U8
   - 7 режимов блендинга: Normal, Screen, Add, Subtract, Multiply, Divide, Difference
   - Per-pixel alpha compositing
   - Enum `CompositorType` с заглушками для GPU варианта (закомментированы)

2. **GPU инфраструктура**:
   - ✅ OpenGL контекст через `egui_glow` v0.33
   - ✅ `ViewportRenderer` активно использует OpenGL для отображения
   - ✅ Шейдерная система с поддержкой кастомных GLSL шейдеров
   - ✅ Поддержка HDR (exposure, gamma)
   - ✅ Асинхронная загрузка текстур через PBO

3. **Интеграция в Project**:
   - Поле `compositor: CompositorType` (runtime-only, не сериализуется)
   - Метод `Project::set_compositor()` для переключения
   - Используется в `Comp::compose()` → `project.compositor.blend_with_dim()`

4. **Settings система**:
   - `AppSettings` в `dialogs/prefs/prefs.rs`
   - Категории: General, UI (можно добавить Performance)
   - Сериализация настроек между сессиями

---

## 🎯 РЕКОМЕНДУЕМЫЙ ПОДХОД

### Вариант: **OpenGL Framebuffer Objects (FBO)**

**Почему именно этот вариант:**
- ✅ Используем уже существующий `glow` контекст
- ✅ Максимальная совместимость (OpenGL 3.0+, поддерживается везде)
- ✅ Минимальные изменения в архитектуре
- ✅ Простая интеграция с текущим рендерером
- ✅ 10-50x ускорение по сравнению с CPU (зависит от разрешения)

**Альтернативы (не рекомендую сейчас):**
- ❌ Compute Shaders - требует OpenGL 4.3+, не везде поддерживается
- ❌ wgpu/WebGPU - дополнительная зависимость, сложная интеграция

---

## 📋 ДЕТАЛЬНЫЙ ПЛАН ИНТЕГРАЦИИ

### **Фаза 1: Добавление GPU композера**

#### 1.1. Создать `GpuCompositor` в `compositor.rs`

```rust
pub struct GpuCompositor {
    gl_context: Arc<glow::Context>,
    fbo: Option<glow::Framebuffer>,
    blend_program: Option<glow::Program>,
    // Кэш текстур для слоёв
    texture_cache: HashMap<u64, glow::Texture>,
}
```

**Методы:**
- `new(gl_context)` - инициализация
- `blend()` - композ через GPU
- `blend_with_dim()` - композ с фиксированным размером холста
- `compile_blend_shader()` - компиляция шейдера для блендинга
- `upload_frame_to_texture()` - загрузка Frame → GPU текстура
- `download_texture_to_frame()` - скачивание результата обратно в Frame

#### 1.2. Шейдеры для композитинга

**Fragment shader** с поддержкой всех blend modes:
```glsl
uniform sampler2D u_bottom;
uniform sampler2D u_top;
uniform float u_opacity;
uniform int u_blend_mode; // 0=normal, 1=screen, 2=add...

void main() {
    vec4 bottom = texture(u_bottom, v_texcoord);
    vec4 top = texture(u_top, v_texcoord);

    vec4 blended = blend(bottom, top, u_blend_mode);
    gl_FragColor = mix(bottom, blended, top.a * u_opacity);
}
```

#### 1.3. Обновить enum `CompositorType`

Раскомментировать GPU вариант:
```rust
pub enum CompositorType {
    Cpu(CpuCompositor),
    Gpu(GpuCompositor), // Разкомментировать
}
```

---

### **Фаза 2: Настройки и UI**

#### 2.1. Добавить поле в `AppSettings`

```rust
// src/dialogs/prefs/prefs.rs
pub struct AppSettings {
    // ...
    pub compositor_mode: CompositorMode, // Новое поле
}

#[derive(serde::Serialize, serde::Deserialize, Clone, Copy)]
pub enum CompositorMode {
    Cpu,
    Gpu,
}
```

#### 2.2. Добавить переключатель в Preferences UI

В `render_ui_settings()` или новую категорию "Performance":
```rust
ui.heading("Compositing");
ui.add_space(8.0);

ui.horizontal(|ui| {
    ui.label("Compositor:");
    ui.radio_value(&mut settings.compositor_mode, CompositorMode::Cpu, "CPU");
    ui.radio_value(&mut settings.compositor_mode, CompositorMode::Gpu, "GPU");
});
ui.label("GPU compositor uses OpenGL for faster multi-layer blending.");
```

---

### **Фаза 3: Передача GL контекста**

#### 3.1. Получить GL контекст в `PlayaApp`

```rust
// src/main.rs
impl eframe::App for PlayaApp {
    fn update(&mut self, ctx: &egui::Context, frame: &mut eframe::Frame) {
        // Получаем GL контекст
        if let Some(gl) = frame.gl() {
            self.update_compositor_from_settings(gl);
        }
        // ...
    }
}
```

#### 3.2. Создать метод обновления композера

```rust
impl PlayaApp {
    fn update_compositor_from_settings(&mut self, gl: &Arc<glow::Context>) {
        let desired = match self.settings.compositor_mode {
            CompositorMode::Cpu => CompositorType::Cpu(CpuCompositor),
            CompositorMode::Gpu => CompositorType::Gpu(
                GpuCompositor::new(gl.clone())
            ),
        };

        // Обновляем, если изменилось
        if !matches_compositor(&self.player.project.compositor, &desired) {
            self.player.project.set_compositor(desired);
            log::info!("Compositor switched to: {:?}", self.settings.compositor_mode);
        }
    }
}
```

---

### **Фаза 4: Тестирование и оптимизация**

#### 4.1. Fallback на CPU при ошибках

```rust
impl GpuCompositor {
    pub fn blend(&self, frames: Vec<(Frame, f32, BlendMode)>) -> Option<Frame> {
        match self.blend_impl(frames.clone()) {
            Ok(result) => Some(result),
            Err(e) => {
                log::error!("GPU compositor failed: {}, falling back to CPU", e);
                CpuCompositor.blend(frames) // Fallback
            }
        }
    }
}
```

#### 4.2. Производительность

- **Измерить время композитинга** через `std::time::Instant`
- **Добавить статистику** в status bar: "Comp: GPU 2.3ms" / "Comp: CPU 45ms"
- **Оптимизировать** texture cache - переиспользовать текстуры между кадрами

#### 4.3. Поддержка всех форматов

Убедиться, что GPU версия работает с:
- F32 (RGBA32F)
- F16 (RGBA16F)
- U8 (RGBA8)

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Алгоритм GPU композитинга:

```
1. Загрузить все слои как текстуры на GPU
2. Создать FBO с выходной текстурой
3. Для каждой пары слоёв (bottom, top):
   a. Привязать FBO с bottom текстурой
   b. Активировать blend shader с параметрами (opacity, blend_mode)
   c. Отрисовать quad с top текстурой
   d. Результат → новая bottom текстура
4. Скачать финальную текстуру обратно в Frame
```

### Управление памятью GPU:

- **Texture cache** - переиспользуем текстуры между кадрами
- **LRU eviction** - удаляем старые текстуры при нехватке VRAM
- **Lazy upload** - загружаем на GPU только при необходимости

---

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

| Проблема | Решение |
|----------|---------|
| Старые GPU без OpenGL 3.0 | Автоматический fallback на CPU |
| Нехватка VRAM | LRU cache + ограничение размера кэша |
| Разные форматы слоёв | Конвертация на GPU через шейдеры |
| Контекст потерян | Пересоздание ресурсов при ошибке |
| Медленная загрузка на GPU | Async upload через PBO (уже есть в renderer) |

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### Производительность:
- **4K композ (3840x2160)**: CPU ~50ms → GPU ~2-5ms (**10-25x быстрее**)
- **2K композ (1920x1080)**: CPU ~15ms → GPU ~1-2ms (**7-15x быстрее**)
- **HD композ (1280x720)**: CPU ~8ms → GPU ~0.5-1ms (**8-16x быстрее**)

### Совместимость:
- ✅ Windows/Linux/macOS
- ✅ NVIDIA/AMD/Intel GPU
- ✅ Fallback на CPU при проблемах

---

## 🚀 ЭТАПЫ ВНЕДРЕНИЯ

### Минимальный MVP (1-2 дня):
1. Создать `GpuCompositor` с простым Normal blend
2. Добавить переключатель в настройках
3. Передать GL контекст и протестировать

### Полная реализация (3-5 дней):
1. Все 7 blend modes
2. Поддержка всех пиксельных форматов
3. Texture cache и оптимизации
4. Fallback логика
5. Тестирование и документация

---

## ❓ ВОПРОСЫ ДЛЯ УТОЧНЕНИЯ

1. **Приоритет производительности vs совместимости?**
   - Если важна максимальная совместимость → OpenGL FBO (рекомендую)
   - Если важна производительность → можно добавить Compute Shaders как опцию

2. **Нужна ли статистика производительности?**
   - Показывать время композитинга в status bar?
   - Добавить debug режим с детальной статистикой?

3. **Fallback стратегия:**
   - Автоматический при ошибках? (рекомендую)
   - Показывать предупреждение пользователю?

4. **Начать с MVP или сразу полная реализация?**

---

## 📝 КЛЮЧЕВЫЕ ФАЙЛЫ ДЛЯ ИЗМЕНЕНИЯ

### Основные:
- `src/entities/compositor.rs` - добавить `GpuCompositor` структуру
- `src/entities/project.rs` - обновить логику переключения
- `src/dialogs/prefs/prefs.rs` - добавить настройку
- `src/main.rs` - передача GL контекста

### Вспомогательные:
- `src/widgets/status/mod.rs` - статистика производительности (опционально)
- `src/config.rs` - настройки по умолчанию (если нужно)

---

## 🎓 ССЫЛКИ НА ДОКУМЕНТАЦИЮ

- [glow crate](https://docs.rs/glow/latest/glow/) - OpenGL bindings
- [egui_glow](https://docs.rs/egui_glow/latest/egui_glow/) - интеграция egui + glow
- [OpenGL Framebuffer Objects](https://www.khronos.org/opengl/wiki/Framebuffer_Object)
- [Blend modes в GLSL](https://www.shadertoy.com/view/XdS3RW)

---

**Дата анализа**: 2025-11-23
**Проект**: Playa Image Sequence Player
**Версия**: 0.1.133
