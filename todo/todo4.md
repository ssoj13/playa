# TODO: 

  - Integration with ShotGrid
  - Python API (and accordingly - text or graphical console)
  - Access via built-in web server
  - ...sequence comparison when you overlay one on top of the other and toggle the top layer on/off) we do that way more often than API and ShotGrid stuff)
  - Video playback from MPEGs or other codecs
  - Cloud integration
  - ...multiple instances running in parallel
  - Sort out the memory issues.
  - Playback from VRAM with real-time color grading.