[package]
name = "playa-prefs"
version = "0.1.142"
edition = "2024"
authors = ["joss13@gmail.com"]
description = "Pluggable Preferences registry + tree-view modal window for egui apps. Each module owns its prefs UI; the host aggregates."
license = "MIT"

[dependencies]
egui = "0.34"
