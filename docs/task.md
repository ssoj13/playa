мы добавим крейт playa-ffmpeg чтобы читать видео.
нужно добавить .mp4 и prores файлы везде - в dragndrop, в файловый диалог, сразу со всеми image файлами.
при открытии видео файлы мы должны применять такую же схему загрузки как и с image sequences, использовать те же dataflow с небольшими различиями:
кадры в Frame можно обозначать добавлением суффикса "@frame": c:/temp/aaa.mp4@17 - кадр 17. Мы можем хранить это значение в отдельных Frame Для загрузки.

В Cache просто добавляется ещё один Sequence("videofile.mp4") - так же как и image sequence. detect должен читать метадату видео и определять кол-во кадров и разрешение, всё остальное как обычно - аллоцируем кадры под placeholders и грузим их в воркерах.
Отличие в том что на каждый Sequence нам нужно держать ffmpeg decoder который будет serve нам нужные кадры.
При скруббинге должно происходить то же что и с сиквенсами - сброс очереди загрузки (через session id?) и загрузка с нового места.
Спроси вопросы, сделай план.


