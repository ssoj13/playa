Изучи код.
Впредь сначала показывай мне свои выводы, возможные варианты исправления и жди моего разрешения.


Давай поправим окно Attributes:
  - сделай так чтобы все поля ввода были выровнены по какой-то общей границе, можно даже поставить туда перемещаемый сплит между колонкой атрибутов и их полей ввода?
  - сделай сортировку по имени атрибута и убедись что положение сплиттера персистентно. Используй нашу систему сериализации - как и для остальных окон, для attributes мы сохраняем какие-то Attributes в общем hashmap сериалиализации. Проверь так ли это, задай вопросы если надо.
  - некоторые наши атрибуты - enums. можешь сделать для них drop-down combo boxes с вариантами?


В окне Project нужно:
  - почему-то все предметы в окне project имеют огромную длину, из-за этого окно имеет крошечный скроллбар. Почему это происходит?
  - Просто выбор обьекта в окне project ничего не делает - посылает сигнал "project.selection_changed". Дабл-клик должен посылать "project.active_changed", и обработчик установки активной Comp должен реагировать на это сообщение. Это значит что теперь можно просто кликать мышкой по items в окне проектов и они не будут меняться. Для показа нужно будет дабл-кликнуть.
  - При этом в окне должна быть чёткая индикация что этот обьект сейчас - текущий в timeline и viewport. Скажем подсветка зелененьким (тонкая вертикальная полоска на левом торце item bar)
  - Этот статус должен сохраняться, быть персистентным. Добавь его в сериализацию окна Project. Скажем в виде атрибута Project.selection и Project.active, которые сохраняются вместе со всеми остальными. Важно - selection - это список. Простым кликом выделяется один обьект, ctrl-click добавляет/убирает обьект из selection по одному, shift-click добавляет все обьекты между последним выбранным и кликнутым. Project.active - это один элемент. И то и то - просто uuid нужных items.
  - Кнопки save/load/add clip/add comp/clear all выстроить в один ряд. Убрать надпись "project" над ними - она уже есть в заголовке окна.
  - 
  - драг-н-дроп надо модифицировать чтобы можно было перетащить сразу стопку клипов (это позже всего)

Сделай отчёт по переделке и жди

